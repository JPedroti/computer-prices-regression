# Exact versions of the environment the project was developed and tested in:
# Python 3.11.9, 64-bit, Windows 11. Install into a fresh Python 3.11
# virtual environment with:  pip install -r requirements.txt
#
# Every third-party package the code imports is pinned, together with the
# numerical back ends shap and scikit-learn compute with and the tooling used
# to execute the notebook. Other transitive dependencies are left to pip.
#
# optuna was installed during development but no code imports it, so it is not
# listed.

# Core
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.0
joblib==1.5.3

# Gradient boosting
lightgbm==4.7.0
xgboost==3.2.0
catboost==1.2.10

# Explainability
shap==0.51.0

# Plots
matplotlib==3.11.1
seaborn==0.13.2

# Numerical back ends used by shap and scikit-learn
numba==0.67.0
llvmlite==0.49.0
threadpoolctl==3.6.0

# Notebook (executed end to end with `python -m nbconvert --execute`)
jupyter==1.1.1
ipykernel==7.3.0
ipython==9.15.0
nbconvert==7.17.1
nbclient==0.11.0
nbformat==5.10.4

# Tests
pytest==9.1.1
